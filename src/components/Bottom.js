import { Text } from "react-native";
import { stylePage } from "../styles/main";

export default function Bottom() {
  return (
    <Text style={stylePage.end}>You have reached the bottom of the page</Text>
  );
}
